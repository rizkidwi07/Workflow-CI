name: telco_churn_ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "telco_churn_preprocessed.csv"}
      n_estimators: {type: int, default: 100}
      max_depth: {type: int, default: 10}
      min_samples_split: {type: int, default: 5}
    command: "python modelling.py --data_path {data_path} --n_estimators {n_estimators} --max_depth {max_depth} --min_samples_split {min_samples_split}"
